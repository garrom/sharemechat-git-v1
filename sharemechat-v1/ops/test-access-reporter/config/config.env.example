# Reporter de accesos de TEST
# Copiar a /etc/sharemechat-test-access-reporter/config.env

PYTHON_BIN=python3

# Entrada: salida del clasificador de TEST
CLASSIFIER_OUTPUT_ROOT=/var/log/sharemechat-test-access-classifier

# Salida: reportes diarios generados manualmente
OUTPUT_ROOT=/var/log/sharemechat-test-access-reporter

# SMTP manual para envio opcional del reporte diario
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_STARTTLS=true
SMTP_TIMEOUT_SECONDS=30
EMAIL_FROM=
EMAIL_TO=
