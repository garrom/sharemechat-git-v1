# Marker para preservar la carpeta vacía en git hasta que se commitee
# el primer PDF versionado del Model Collaboration Agreement.
#
# Convención de archivado: ver ../README.md
#
# La carpeta arranca limpia desde la próxima versión publicada (v4.2)
# en adelante. Las versiones anteriores (datos fake) no se rescatan: la
# reproducibilidad autoritativa de texto via este repo aplica de la v4.2
# en adelante.
