# Secretos del vigilante de salud (obs #5). Copiar a /etc/sharemechat-health-monitor/secrets.env
# (permisos 600, root). Se carga por `source` desde bin/check-health.sh y se
# EXPORTA al entorno del python (nunca por argv).
SMTP_PASSWORD=
