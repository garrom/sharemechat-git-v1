// jest-dom añade matchers custom para assertions sobre el DOM
// (ej: toBeInTheDocument, toHaveTextContent, toHaveAttribute).
// react-scripts detecta este archivo automáticamente antes de cada
// suite de tests (patrón CRA estándar).
import '@testing-library/jest-dom';
