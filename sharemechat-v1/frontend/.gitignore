# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
# builds por superficie/entorno generados para deploy manual a S3 (PRO-7)
build-*-prod/
# carpetas de salida del script ops/scripts/deploy-frontend.ps1 (blindaje
# rev 2026-06: una carpeta por superficie via env BUILD_PATH de CRA, para
# que builds product y admin no compartan jamas la misma ubicacion)
/build-product
/build-admin

# lock file del script de deploy de frontend para impedir invocaciones
# concurrentes que pisen la misma carpeta de build
.deploy.lock

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Mock/MSW solo local
src/mocks/
public/mockServiceWorker.js
